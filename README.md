# Katarzyna_chalas_page
## Teksty strony

`teksty-strony.xlsx` — wzorzec do wypełnienia przez klienta (kolumna „NOWY TEKST”).
Regeneracja po zmianach w `index.html`: `python3 tools/make_content_template.py`
(samotest: `--check`). Wymaga `python3-openpyxl`.
